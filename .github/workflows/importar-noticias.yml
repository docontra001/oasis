name: Importar Notícias

on:
  schedule:
    # Executa a cada hora
    - cron: "0 * * * *"

  workflow_dispatch:

jobs:
  importar:
    runs-on: ubuntu-latest

    steps:
      - name: Chamar API do OASIS
        run: |
          curl -X GET "${{ secrets.OASIS_CRON_URL }}"